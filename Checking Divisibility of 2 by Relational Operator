#include <stdio.h>

int main()

{
    int x;

    printf("enter the number : ");
    scanf("%d",&x);

    /* 1 = divisible by 2
    0 = not divisible by 2*/

    printf("%d",x%2==0);

    return 0;
}
